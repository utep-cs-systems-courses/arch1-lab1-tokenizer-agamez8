# list object files
OBJS= simpleUI.o tokenizer.o history.o

CFLAGS=-g -O3 # -g for debug, -03 for optimization

all: tokenizer

tokenizer: $(OBJS)
	cc -o tokenizer $(CFLAGS) $(OBJS)

# delete files generated by compiler
clean:
	rm -f *.o tokenizer

run:tokenizer
	./tokenizer
